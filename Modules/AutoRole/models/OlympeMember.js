class OlympeMember {
    /**
     *
     * @param {OlympeUser} user
     * @param {Object} tags
     * @param {Array} roles
     */
    constructor(user, tags, roles) {
        this.user = user;
        this.tags = { gameRoles };
        this.roles = roles;
    }
}